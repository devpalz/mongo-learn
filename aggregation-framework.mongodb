/**
 Say a 'report' on a seller, we must aggregrate data to produce the report.

It is an alternative to the find method

It is all about taking a collection, and then running a pipeline of steps on it.
 */